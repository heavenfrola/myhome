<!-- 반복문시작 -->
<ul class="usable_cpn">
<!-- 반복구문시작 -->
	<li>
		<label>
			<span class="check">{{$선택체크박스}}</span>
			<strong>{{$쿠폰명}}</strong><br>{{$할인율}}
			{{if(중복사용불가)}}
			<span class="overlap">(중복사용불가)</span>
			{{endif(중복사용불가)}}
			{{if(사용만료일)}}
			<span class="expiry">~{{$사용만료일}}</span>
			{{endif(사용만료일)}}
		</label>
	</li>
<!-- 반복구문끝 -->
</ul>
<!-- 반복문끝 -->
<!-- 데이터없음시작 -->
<!-- 데이터없음끝 -->