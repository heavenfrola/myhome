<?PHP

	/* +----------------------------------------------------------------------------------------------+
	' |  윙Mobile 매장분류 설정 처리
	' +----------------------------------------------------------------------------------------------+*/

	if($cfg['mobile_use'] != 'Y') msg("먼저 {$cfg['mobile_name']}을 신청하셔야합니다");
	$no = $_POST['no'];
	foreach($no as $key => $val) {
		$pdo->query("update `{$tbl['category']}` set `mobile_hidden` = '{$val}' where `no`='{$key}'");
	}

	msg('\n변경되었습니다.\t\n\n');

?>